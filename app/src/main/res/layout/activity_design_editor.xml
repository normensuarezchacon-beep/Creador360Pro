<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#1A1A1A">

    <LinearLayout
        android:id="@+id/llToolbar"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:background="#2A2A2A"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingHorizontal="4dp">

        <Button android:id="@+id/btnAdd" android:layout_width="36dp" android:layout_height="32dp" android:text="+" android:textSize="16sp" android:backgroundTint="#8B5CF6" android:minWidth="0dp" android:padding="0dp" />
        <Button android:id="@+id/btnLayers" android:layout_width="50dp" android:layout_height="32dp" android:text="Capas" android:textSize="10sp" android:layout_marginStart="2dp" android:backgroundTint="#555555" android:minWidth="0dp" android:padding="0dp" />
        <Button android:id="@+id/btnProperties" android:layout_width="44dp" android:layout_height="32dp" android:text="Prop." android:textSize="10sp" android:layout_marginStart="2dp" android:backgroundTint="#555555" android:minWidth="0dp" android:padding="0dp" />
        <Button android:id="@+id/btnTemplates" android:layout_width="44dp" android:layout_height="32dp" android:text="Plant." android:textSize="10sp" android:layout_marginStart="2dp" android:backgroundTint="#555555" android:minWidth="0dp" android:padding="0dp" />
        <Button android:id="@+id/btnLoadProject" android:layout_width="44dp" android:layout_height="32dp" android:text="Abrir" android:textSize="10sp" android:layout_marginStart="2dp" android:backgroundTint="#555555" android:minWidth="0dp" android:padding="0dp" />

        <View android:layout_width="0dp" android:layout_height="1dp" android:layout_weight="1" />

        <Button android:id="@+id/btnUndo" android:layout_width="32dp" android:layout_height="32dp" android:text="↩" android:textSize="14sp" android:backgroundTint="#555555" android:minWidth="0dp" android:padding="0dp" />
        <Button android:id="@+id/btnRedo" android:layout_width="32dp" android:layout_height="32dp" android:text="↪" android:textSize="14sp" android:layout_marginStart="2dp" android:backgroundTint="#555555" android:minWidth="0dp" android:padding="0dp" />
        <Button android:id="@+id/btnSave" android:layout_width="50dp" android:layout_height="32dp" android:text="Guard." android:textSize="10sp" android:layout_marginStart="2dp" android:backgroundTint="#555555" android:minWidth="0dp" android:padding="0dp" />
        <Button android:id="@+id/btnExport" android:layout_width="55dp" android:layout_height="32dp" android:text="Exp." android:textSize="10sp" android:layout_marginStart="2dp" android:backgroundTint="#8B5CF6" android:minWidth="0dp" android:padding="0dp" />
    </LinearLayout>

    <com.creador360pro.ui.editor.CanvasView
        android:id="@+id/canvasView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <LinearLayout
        android:id="@+id/llCapas"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:background="#2A2A2A"
        android:orientation="vertical"
        android:visibility="gone"
        android:padding="4dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="18dp"
            android:text="CAPAS (toca para seleccionar, ✏️ para editar)"
            android:textColor="#FFFFFF"
            android:textSize="10sp"
            android:textStyle="bold"
            android:gravity="center_vertical" />

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">

            <LinearLayout
                android:id="@+id/llCapasContainer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical" />
        </ScrollView>
    </LinearLayout>
</LinearLayout>
